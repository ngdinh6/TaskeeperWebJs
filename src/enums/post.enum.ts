export enum JobType {
    FREELANCE = "freelance",
    FULLTIME = "fulltime",
    PART_TIME = "parttime",
}

export enum CandidateStatus {
    APPLIED = "applied",
    APPROVED = "approved",
    CHARGED = "charged",
    CANCELED = "canceled",
}
